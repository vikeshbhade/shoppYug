
<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register ui form">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'Register'|trans({}, 'FOSUserBundle') }}"  class="ui button"/>
    </div>
</form>

<p>Already a Member<a href="{{ path('fos_user_security_login') }}">Login</a></p>

